Review the following code for real bugs only. Use the examples below to calibrate your judgment.

EXAMPLE 1 — Flag this (real bug):
Code: const user = await db.query('SELECT * FROM users WHERE id = ' + req.params.id)
Correct finding: {"file":"routes.js","line":8,"severity":"HIGH","issue":"SQL injection — user input concatenated directly into query string","suggestedFix":"Use parameterized query: db.query('SELECT * FROM users WHERE id = $1', [req.params.id])"}

EXAMPLE 2 — Skip this (cosmetic only):
Code: const usr = await getUserById(id) // some devs write 'user' instead
Correct action: SKIP — variable naming preference is not a bug

EXAMPLE 3 — Flag this (real bug):
Code: app.delete('/users/:id', async (req, res) => { await deleteUser(req.params.id) })
Correct finding: {"file":"routes.js","line":14","severity":"HIGH","issue":"No auth middleware on DELETE route — unauthenticated callers can delete any user","suggestedFix":"Add authMiddleware: app.delete('/users/:id', authMiddleware, async (req, res) => {...})"}

EXAMPLE 4 — Skip this (cosmetic only):
Code: // TODO: add better error message here
Correct action: SKIP — comment quality is not a bug

Now review the following files using the same judgment. Output as JSON array with fields: file, line, severity, issue, suggestedFix.
